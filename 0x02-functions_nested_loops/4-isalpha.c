#include "main.h"
/**
 *_isalpha - prints the alphabet in lowercase.
 *@c: The char is checked
 *Return: 1 if character is lowercase 0
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') ||(c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
